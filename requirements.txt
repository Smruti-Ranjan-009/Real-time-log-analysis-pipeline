# Phase 1 — ingestion pipeline
fastapi==0.109.2
uvicorn[standard]==0.27.1
kafka-python==2.0.2
psycopg2-binary==2.9.9
python-dotenv==1.0.1
pydantic==2.6.1

# Phase 2 — intelligence layer
sentence-transformers==2.7.0
qdrant-client==1.9.1
groq==0.9.0

# requirements_phase3_additions.txt
# Add these to your existing requirements.txt
# (prometheus-client is the only mandatory new dependency)

prometheus-client==0.20.0   # Prometheus instrumentation